<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Signup</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(135deg, #7d89f5, #aab2fb);
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    .signup-card {
      background-color: #f5f7ff;
      padding: 2rem;
      border-radius: 16px;
      box-shadow: 0 8px 20px rgba(125, 137, 245, 0.3);
      width: 100%;
      max-width: 450px;
    }

    .signup-card h2 {
      color: #5f6de0;
      margin-bottom: 1.5rem;
      text-align: center;
    }

    .form-control {
      border-radius: 10px;
      border: 1px solid #c9cefa;
    }

    .form-control:focus {
      border-color: #7d89f5;
      box-shadow: 0 0 0 0.2rem rgba(125, 137, 245, 0.25);
    }

    .btn-primary {
      background: linear-gradient(90deg, #7d89f5, #5f6de0);
      border: none;
      border-radius: 10px;
      transition: 0.3s ease;
    }

    .btn-primary:hover {
      background: linear-gradient(90deg, #5f6de0, #3f4dcf);
    }
  </style>
</head>
<body>

  <div class="signup-card">
    <h2>Create an Account</h2>
    <form action="/signup" method="POST">
      <div class="mb-3">
        <input type="text" name="name" class="form-control" placeholder="Full Name" required />
      </div>
      <div class="mb-3">
        <input type="email" name="email" class="form-control" placeholder="Email Address" required />
      </div>
      <div class="mb-3">
        <input type="password" name="password" class="form-control" placeholder="Password" required />
      </div>
      <div class="mb-3">
        <input type="text" name="designation" class="form-control" placeholder="Designation (e.g., Architect Engineer)" required />
      </div>
      <div class="mb-3">
        <input type="url" name="profileUrl" class="form-control" placeholder="Profile Photo URL" required />
      </div>
      <button type="submit" class="btn btn-primary w-100">Sign Up</button>
    </form>
  </div>

</body>
</html>